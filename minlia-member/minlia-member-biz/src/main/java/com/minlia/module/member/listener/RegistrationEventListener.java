package com.minlia.module.member.listener;
//
//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.context.event.EventListener;
//import org.springframework.stereotype.Component;
//
///**
// * @author garen
// */
//@Component
//public class RegistrationEventListener {
////
////
////    /**
////     * 当调用源有事务的时候才需要使用TransactionalEventListener, 无事务的时候是不会调用的, 需要使用EventListener
////     * @param event
////     */
////    @EventListener
////    public void onComplete(RegistrationEvent event) {
////        User user = (User) event.getSource();
////        if (user.getDefaultRole().equals(RoleEnum.USER.getCode())) {
////        }
////    }
//
//}
